// const mongoose = require('mongoose');

// //mongoose.Promise = global.Promise; // - not needed in new version i believe
// if (process.env.MONGODB_URI) {
//   mongoose.connect(process.env.MONGODB_URI, { useNewUrlParser: true })
// } else {
//   mongoose.connect('mongodb://localhost:27017/dict', { useNewUrlParser: true })
// }
// // mongoose.connect(process.env.MONGODB_URI, { useNewUrlParser: true } || 'mongodb://localhost:27017/dict', { useNewUrlParser: true })

// module.exports = {
//   mongoose
// }
